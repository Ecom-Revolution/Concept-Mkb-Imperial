<!-- Bloc Livraison estimée avec dates dynamiques -->
<div id="delivery-estimate" style="border:1px dashed #ccc; padding:15px; margin-top:20px; border-radius:8px;">
  <p style="font-weight:bold; font-size:16px; margin-bottom:10px;">
    📦 Commandez maintenant et recevez votre colis entre <span id="delivery-range"></span>
  </p>
  <div style="display:flex; justify-content:space-around; text-align:center;">
    <div>
      <img src="https://img.icons8.com/ios-filled/50/000000/shopping-bag.png" width="30" alt="Commande reçue"><br>
      <strong>Commande reçue</strong><br><span id="order-date"></span>
    </div>
    <div>
      <img src="https://img.icons8.com/ios-filled/50/000000/delivery.png" width="30" alt="Commande expédiée"><br>
      <strong>Préparation</strong><br><span id="prep-date"></span>
    </div>
    <div>
      <img src="https://img.icons8.com/ios-filled/50/000000/marker.png" width="30" alt="Livraison"><br>
      <strong>Livraison</strong><br><span id="delivery-date"></span>
    </div>
  </div>
</div>

<!-- Bloc Paiement sécurisé -->
<div style="margin-top:25px; padding:15px; border:1px solid #ccc; border-radius:8px;">
  <p style="font-weight:bold; font-size:16px; margin-bottom:10px;">🔐 Paiement 100% sécurisé avec :</p>
  <img src="https://i.imgur.com/jUqqJ4R.png" alt="Paiement sécurisé - cartes bancaires" style="max-width:100%; height:auto;">
</div>

<script>
  const mois = ["Jan", "Fév", "Mar", "Avr", "Mai", "Juin", "Juil", "Août", "Sep", "Oct", "Nov", "Déc"];

  const now = new Date();
  const orderDate = new Date(now);
  const prepDate = new Date(now.setDate(orderDate.getDate() + 1));
  const deliveryStart = new Date(now.setDate(prepDate.getDate() + 1));
  const deliveryEnd = new Date(now.setDate(deliveryStart.getDate() + 2));

  function formatDate(date) {
    return date.getDate() + " " + mois[date.getMonth()];
  }

  document.getElementById("order-date").textContent = formatDate(orderDate);
  document.getElementById("prep-date").textContent = formatDate(prepDate);
  document.getElementById("delivery-date").textContent = formatDate(deliveryEnd);
  document.getElementById("delivery-range").textContent = formatDate(deliveryStart) + " et " + formatDate(deliveryEnd);
</script>
